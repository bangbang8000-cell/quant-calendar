# 开发/CI 依赖（约束到运行时锁文件，保证开发与生产同版本）
# 重新生成: uv pip compile --universal -c requirements.lock requirements-dev.in -o requirements-dev.lock
-c requirements.lock

pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.6.0
httpx>=0.27.0
playwright>=1.40.0
