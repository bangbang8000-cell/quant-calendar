# 量化选股日历 直接依赖 (v3.10 起使用 uv pip compile 生成 requirements.lock)
# 仅声明顶层直接依赖；传递依赖由 uv 解析并锁定到 requirements.lock。
# 修改依赖后重新生成: uv pip compile --universal requirements.in -o requirements.lock

fastapi>=0.100.0
uvicorn>=0.23.2
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0
python-dotenv>=1.0.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
# V4.9.2: pydantic 2.13.5 刚发布尚未在 PyPI CDN 全量传播, uv 重编译解析不确定(2.13.4/2.13.5 抖动致 CI 锁漂移门禁红);
# 暂排除 2.13.5 稳定锁文件(与生产运行版本一致); 传播完成后移除上界
pydantic>=2.0.0,<2.13.5
pydantic-settings>=2.0.0
slowapi>=0.1.9
aiofiles>=23.0.0
# V4.2 (FR-4.2.3): 实时报价 WebSocket — FastAPI WS 需要 websockets/wsproto (uvicorn[standard])
# websockets<14 保留 legacy.handshake (uvicorn 0.47 websockets_impl 依赖)
websockets>=12.0,<14
wsproto>=1.2.0
tushare>=1.4.0
cryptography>=41.0.0
pypinyin>=0.55.0
