# TEST-PLAN v6.9.3：存量优化整改测试计划

> 版本：v1.0（待用户终审授权）
> 状态：待批准
> 创建日期：2026-09-12
> 需求来源：《PRD-v6.9.3.md》《DEV-PLAN-v6.9.3.md》
> 前置：DEV-PLAN-v6.9.3.md（本计划独立可评审）

---

## 1 测试策略

| 层 | 手段 | 覆盖对象 |
|---|---|---|
| L1 门禁 | pytest | 菜单契约、i18n 缺词、AppIcon 映射、暗色 token、一致性（12 项改动回归） |
| L2 单元 | pytest + node | 主题 hue 工具抽取、StockList 新开关渲染、_cache LRU、consensus join 行情、R1 过滤 |
| L3 接口 | pytest（mock） | `/api/alerts/history` 面板链路、sxsc test_connection 重建/超时、shortterm overview 并行、功能配置字段 |
| L4 性能 | pytest + 手动 | 短线复盘预取缓存命中、列表行渲染体积 |
| L5 视觉 | 浏览器实测 | 三处股票列表信息密度、下拉按钮、搜索框右移、铃铛面板、主题面板、日历工具栏、导航形态面板 |
| L6 回归 | 全量目标集 | 无新增失败 |

---

## 2 用例明细

### 2.1 菜单契约与配置（F12 / A8）

| ID | 用例 | 预期 |
|---|---|---|
| TC-6.9.3.01 | 系统配置 subPages 顺序 | `config→feature→autoeval→datasource→user→about→notification` |
| TC-6.9.3.02 | guestSubPages 同步 | 仅含 config/about（或按既有策略）且顺序一致 |
| TC-6.9.3.03 | subPageNames 全量覆盖 | 新序下每个 key 均有中文名，无缺词 |

### 2.2 股票列表信息密度（F1 / A1）

| ID | 用例 | 预期 |
|---|---|---|
| TC-6.9.3.04 | 后端 consensus 返回价格字段 | `get_strategy_consensus` 结果含 `price/change_pct`（无行情时字段缺失而非报错） |
| TC-6.9.3.05 | TOP5 行渲染 | StockList 显示共识徽章+进度条+≤3 标签+价格列；空行情优雅降级 |
| TC-6.9.3.06 | 共识榜/日历股票池统一 StockList | 两处改用组件后结构与 TOP5 一致，虚拟滚动可用 |
| TC-6.9.3.07 | 标签 +N 折叠 | 策略数 >3 时第三个标签显示 `+N` |

### 2.3 下拉按钮 / 搜索框 / 日历工具栏（F2 F3 F7）

| ID | 用例 | 预期 |
|---|---|---|
| TC-6.9.3.08 | el-select 长选项 | 文字省略号不换行、箭头同排 |
| TC-6.9.3.09 | 工具栏 el-select 宽度 | 不被 flex 拉伸，内容自适应 |
| TC-6.9.3.10 | 搜索框右移 | 桌面贴近铃铛，移动端行为不回归 |
| TC-6.9.3.11 | 日历工具栏三段排布 | 视图切换最左；上一/下一/对比最右；切换视图与导航逻辑正常 |

### 2.4 顶部栏面板（F4 F5 F6）

| ID | 用例 | 预期 |
|---|---|---|
| TC-6.9.3.12 | 铃铛点击开面板 | 展示最近投递历史（limit 8）；空态「暂无通知」；加载/失败态正常 |
| TC-6.9.3.13 | 面板跳转通知中心 | 底部入口跳转 系统配置-通知中心 并关闭面板 |
| TC-6.9.3.14 | 导航形态面板背景 | 不透明、边框/阴影/圆角与用户菜单一致；三形态切换生效 |
| TC-6.9.3.15 | 主题面板 | 模式切换（浅/深/跟随）+ 6 色板 + 自定义 slider 均即时生效并持久化 |
| TC-6.9.3.16 | 主题逻辑共享 | Header 与功能配置子页 hue 操作结果一致（同一 state） |

### 2.5 短线复盘提速（F8 / B1）

| ID | 用例 | 预期 |
|---|---|---|
| TC-6.9.3.17 | 进入页面预取 | overview/pools/lhb 并行请求，切子页命中缓存秒开 |
| TC-6.9.3.18 | _cache LRU | 超过 50 条淘汰最旧，无内存增长 |
| TC-6.9.3.19 | overview 聚合并行化 | 后端响应时长不劣化；store 缓存 60s 命中 |

### 2.6 AI / 数据源 / 功能配置（F9 F10 F11）

| ID | 用例 | 预期 |
|---|---|---|
| TC-6.9.3.20 | R1 目录过滤 | 预置目录与用户自定义模型含 `r1/R1` 条目均被过滤；迁移幂等 |
| TC-6.9.3.21 | 默认模型更新 | ai.js/auth.js 默认 `deepseek-v4-flash`，向导表单同步 |
| TC-6.9.3.22 | sxsc 客户端缺失重建 | 清除 `_clients` 后调用 test_connection 仍成功（10s 超时） |
| TC-6.9.3.23 | sxsc 错误分类 | 未初始化/超时/token 无效返回不同 message |
| TC-6.9.3.24 | 前端先保存再测试 | 解锁编辑态点测试→先 POST 保存再 POST 测试 |
| TC-6.9.3.25 | 数据刷新精简 | 自选池拉取 UI/字段移除；手动+定时刷新仍可用 |
| TC-6.9.3.26 | 策略研究菜单恒显 | researchMenuEnabled 移除后菜单始终显示，无引用残留 |

### 2.7 回归（L6）

| ID | 用例 | 预期 |
|---|---|---|
| TC-6.9.3.27 | 既有门禁全量 | 菜单契约/i18n/AppIcon/暗色 token/一致性无新增失败 |
| TC-6.9.3.28 | 版本门禁 | 接口返回 6.9.3，dist 重建产物一致 |

---

## 3 回归范围与已知失败

- 全量 pytest 目标集；记录 pre-existing 失败（`test_transition_tokens` 等既有项）不计入本次回归。
- 视觉项（L5）以浏览器硬刷新实测为准，覆盖亮/暗主题与三导航形态。
